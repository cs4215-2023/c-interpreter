struct {
	int a;
} test;

void main() {
int a = 1;
a++;
int* b;
*b;
&b;

  int arr[5] = { 1, 2, 3, 4, 5 };
  int *ptr = arr;
  ptr++;
  arr++;

1 % 2;
1 / 2;
1 + 2;
1 - 2;
1 >> 1;
1 << 2;

a += 3;
a -= 2;
  char full_name[] = "Drex";
for(int i = 0; i < strlen(full_name); i++) {
  printf("%c", full_name[i]);
}

char* string = "hello";
}